name: Client CI

on:
  pull_request:

jobs:
  client:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: client

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: npm ci

      # - name: Lint         disabled till next's new version isn't working fine with rest environment
      #   run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
